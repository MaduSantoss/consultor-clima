# 🌦️ Consulta de Clima em Python

Projeto prático desenvolvido para consumir a API do **OpenWeatherMap**. O script solicita o nome de uma cidade e retorna a temperatura atual e a descrição do clima (em português).

## 🚀 Tecnologias Usadas

* **Python 3**
* **Requests** (Para fazer a requisição HTTP)
* **Python-Dotenv** (Para gerenciar variáveis de ambiente com segurança)

## 📦 Como rodar o projeto

### 1. Pré-requisitos
Você precisará de uma chave de API (API Key) gratuita do [OpenWeatherMap](https://openweathermap.org/).

### 2. Instalação
Clone este repositório e instale as dependências listadas:

```bash
# Instalar as bibliotecas necessárias
pip install -r requirements.txt
````

### 3\. Configuração

Crie um arquivo chamado `.env` na raiz do projeto (use o `.env.example` como base) e adicione sua chave:

```ini
API_KEY=sua_chave_da_api_aqui
```

### 4\. Execução

Basta rodar o comando:

```bash
python clima.py
```

-----

*Este projeto faz parte da minha jornada de aprendizado em Python.*

````

---